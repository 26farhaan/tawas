const API_URL = "https://127.0.0.1:8000";
// const API_LOCAL_URL = "";

const ENDPOINTS = {
  LOGIN: `${API_URL}/login`,
  TOKEN: `${API_URL}/token`,
  ME: `${API_URL}/me`,
};

export default ENDPOINTS;
